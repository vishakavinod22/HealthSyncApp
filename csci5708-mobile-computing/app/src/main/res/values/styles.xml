<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CalendarDateStyle" parent="@android:style/TextAppearance.DeviceDefault.Small">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/calendar_date_color</item>
        <!-- You may have custom attributes for padding, background, etc. -->
        <item name="android:padding">4dp</item>
        <item name="android:background">@drawable/calendar_date_background</item>
    </style>
    
</resources>
